diff --git a/node_modules/expo/node_modules/expo-constants/ios/EXConstants.podspec b/node_modules/expo/node_modules/expo-constants/ios/EXConstants.podspec
index c4f3b6b..dd68e1b 100644
--- a/node_modules/expo/node_modules/expo-constants/ios/EXConstants.podspec
+++ b/node_modules/expo/node_modules/expo-constants/ios/EXConstants.podspec
@@ -34,10 +34,12 @@ Pod::Spec.new do |s|
     s.source_files = "**/*.{h,m,swift}"
   end
 
-  env_vars = ENV['PROJECT_ROOT'] ? "PROJECT_ROOT=#{ENV['PROJECT_ROOT']} " : ""
+  env_vars = ENV['PROJECT_ROOT'] ? "PROJECT_ROOT='#{ENV['PROJECT_ROOT']}' " : ""
   script_phase = {
     :name => 'Generate app.config for prebuilt Constants.manifest',
-    :script => "bash -l -c \"#{env_vars}$PODS_TARGET_SRCROOT/../scripts/get-app-config-ios.sh\"",
+    # Single-quote the script path so a project path containing spaces isn't
+    # word-split by the inner `bash -l -c` command string.
+    :script => "bash -l -c \"#{env_vars}'$PODS_TARGET_SRCROOT/../scripts/get-app-config-ios.sh'\"",
     :execution_position => :before_compile
   }
   # :always_out_of_date is only available in CocoaPods 1.13.0 and later
